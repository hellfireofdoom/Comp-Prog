#define ll long long
#include <iostream>
#include <string>
#include <vector>

using namespace std;

#ifndef ONLINE_JUDGE
#define debug(x) cerr << #x << " " << x << endl;
#else
#define debug(x);
#endif


signed main(){
    #ifndef ONLINE_JUDGE
        freopen("Error/txt", "w", stderr);
    #endif
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int x = 5;
    debug(x);


}